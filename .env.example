# Copy to .env and fill in.

PORT=3000

# Any PostgreSQL 12+ database.
DATABASE_URL=postgres://localhost:5432/mediconnect

# Used to sign JWTs. Generate one with: openssl rand -base64 32
JWT_SECRET=replace-with-a-long-random-string
